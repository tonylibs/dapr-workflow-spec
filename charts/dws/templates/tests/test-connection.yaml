apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dws.fullname" . }}-test-connection"
  labels:
    {{- include "dws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
