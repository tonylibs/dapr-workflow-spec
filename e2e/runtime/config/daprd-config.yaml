# Shared sidecar configuration: sqlite name resolution over the shared /nr volume,
# because the default mDNS resolver cannot cross container boundaries.
apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: e2e-runtime
spec:
  nameResolution:
    component: sqlite
    version: v1
    configuration:
      connectionString: /nr/resolution.db
