{
  "openapi": "3.0.3",
  "info": { "title": "Relative Server", "version": "1.0.0" },
  "servers": [{ "url": "/inventory/v1" }],
  "paths": {
    "/orders": {
      "get": {
        "operationId": "listOrders",
        "parameters": [
          { "name": "limit", "in": "query", "schema": { "type": "integer" } }
        ],
        "responses": { "200": { "description": "ok" } }
      }
    }
  }
}
