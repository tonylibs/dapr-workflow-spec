{
  "openapi": "3.0.3",
  "info": { "title": "Trailing Slash Server", "version": "1.0.0" },
  "servers": [{ "url": "https://api.x.com/v2/" }],
  "paths": {
    "/pets/{petId}": {
      "get": {
        "operationId": "getPet",
        "parameters": [
          { "name": "petId", "in": "path", "required": true, "schema": { "type": "integer" } }
        ],
        "responses": { "200": { "description": "ok" } }
      }
    }
  }
}
