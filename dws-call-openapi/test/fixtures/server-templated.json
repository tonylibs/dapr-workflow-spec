{
  "openapi": "3.0.3",
  "info": { "title": "Templated Server", "version": "1.0.0" },
  "servers": [{ "url": "https://{region}.api.x.com/v1" }],
  "paths": {
    "/pets/{petId}": {
      "get": {
        "operationId": "getPet",
        "parameters": [
          { "name": "petId", "in": "path", "required": true, "schema": { "type": "integer" } }
        ],
        "responses": { "200": { "description": "ok" } }
      }
    }
  }
}
