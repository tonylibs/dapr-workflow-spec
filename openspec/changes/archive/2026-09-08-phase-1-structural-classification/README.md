# phase-1-structural-classification

Phase 1 second half: V2StructuralCompiler's recursive classification pass — CompiledNode tree,
derived identifiers sanitized to DNS-1123 app IDs, `-fn` naming-collision rule, duplicate-id
rejection, per-node specText, and golden tests against the architecture spec's 6 worked examples
(ADR 0001, 0002, 0003).
