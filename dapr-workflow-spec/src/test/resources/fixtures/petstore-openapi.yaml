document:
  dsl: '1.0.0'
  namespace: default
  name: petstore
  version: '1.0.0'
do:
  - findPet:
      call: openapi
      with:
        document:
          endpoint: http://petstore.local/openapi.json
        operationId: findPetById
        parameters:
          petId: 42
