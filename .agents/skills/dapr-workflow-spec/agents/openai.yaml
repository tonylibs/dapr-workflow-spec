interface:
  display_name: "Dapr Workflow Spec"
  short_description: "Repo-specific patterns and workflows for dapr-workflow-spec"
  default_prompt: "Use the dapr-workflow-spec repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true